# 角色与边界
你是 Claude，仅作为我的开发工具。你不参与任何学生端业务逻辑，不直接回答物理问题，所有 AI 对话能力只能通过后端调用的 Mimo 和 DeepSeekV4Pro 实现。

# 项目概述
计算机专业学生，独立开发一个高中物理 AI 助学 Web 项目。技术栈：Vue3 + FastAPI + SQLite。免费云端部署（如 Vercel + Railway）。包含四大核心模块：多模态答疑、AI 错题本、智能刷题、虚拟仿真实验室。

# 硬性规则
1. 双模型架构：
   - Mimo：负责多模态识图、拍题、基础简答（L1层）
   - DeepSeekV4Pro：负责物理深度答疑、错题归类、变式出题、学情分析（L2层）
2. 分层 AI 调度：后端必须实现 L1→L2 调度逻辑（L1 置信度低或用户要求深度时才调 L2）
3. Claude 不得以任何形式替代上述两个模型，不得生成学生端可见的 AI 回答内容。
4. 你只能输出：架构设计、代码实现、配置文件、部署说明、以及对我需求的澄清提问。

# 你需要交付的内容（逐步进行）
1. 后端完整项目结构（FastAPI + SQLite），包含：
   - 双模型调度器（Mimo / DeepSeekV4Pro 的 API 封装与 fallback 逻辑）
   - 四大模块的路由与 Service 层（多模态答疑、错题本、刷题推荐、仿真实验室记录）
   - 数据库模型（用户、错题本、学情分析、仿真会话）
2. 前端核心页面（Vue3 + Vite）：
   - 聊天/拍题页（支持图片上传）
   - 错题本页（分组展示、变式出题按钮）
   - 智能刷题页（题目渲染、答案对比）
   - 虚拟实验室页（嵌入 iframe 或 WebGL 容器）
3. 免费部署方案（Vercel + Railway）的完整配置与脚本。
4. Mock API 实现（用于开发阶段，避免真实调用 Mimo/DeepSeek 产生费用）。

# 启动方式 —— 请使用 "grill me" skill
不要立刻写代码。首先，你作为 Claude，通过提问方式向我逐项澄清以下关键信息（至少 8 个问题）：
- 我的真实 API 可用情况（是否有 Mimo / DeepSeek 密钥？若无，是否接受先完全用 Mock？）
- 物理题库的来源（我是否提供初始 JSON？或需要你用 DeepSeek 生成 30 道基础题？）
- 虚拟实验室的具体需求（使用现有开源仿真库如 PhET？还是需要你从头写一个简单 Canvas 实验？）
- 错题本的“变式出题”是否需要保存多次生成的变式记录？
- 学情分析的具体输出形式（雷达图？文字建议？）
- 用户身份机制（是否需要登录？还是临时 session？）
- 多模态图片的大小限制与压缩要求
- 对响应速度的期望（是否接受 DeepSeek 的 5-10 秒延迟？）

每问一个问题，等我回答后，再继续下一个。全部澄清后，你才开始分阶段输出代码和文档。

# 补充约束
- 所有代码必须带有详细注释，符合 FastAPI 和 Vue3 最佳实践。
- SQLite 使用 aiosqlite 异步驱动。
- 前端与后端通过 REST API 通信，并处理 CORS。
- 严禁在代码中硬编码任何 API 密钥。
- 最终输出应包含一个 README.md，说明如何本地运行、Mock 模式切换、部署步骤。

请从第一个问题开始。